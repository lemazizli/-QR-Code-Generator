# -QR-Code-Generator
This script generates a QR code from a user-provided URL using the pyqrcode library. The generated QR code is saved as an SVG file, with customizable scale for quality. Users can scan it to quickly access the URL on any device.
